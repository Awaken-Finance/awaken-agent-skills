# ============================================================
# Awaken Agent Kit - Environment Variables
# ============================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# Config priority (high → low):
#   1. Function params (SDK callers pass config directly)
#   2. CLI args (--network, --slippage)
#   3. MCP env block (mcp.json env: {})
#   4. Environment variables (these)
#   5. .env file (this file)
#   6. Code defaults

# ---- Required for trade/swap/liquidity operations ----
AELF_PRIVATE_KEY=

# ---- Network selection: mainnet (default) or testnet ----
# AWAKEN_NETWORK=mainnet

# ---- Override individual endpoints (optional) ----
# Use these to point to your own node or a custom API.
# If not set, defaults for the selected network are used.
# AWAKEN_RPC_URL=https://tdvv-public-node.aelf.io
# AWAKEN_API_BASE_URL=https://app.awaken.finance
# AWAKEN_SOCKET_URL=https://app.awaken.finance/signalr-hubs/trade
# AWAKEN_EXPLORER_URL=https://aelfscan.io/tDVV

# ---- Override contract addresses (optional, advanced) ----
# AWAKEN_TOKEN_CONTRACT=
# AWAKEN_SWAP_HOOK_CONTRACT=

# ---- Trading defaults (optional) ----
# AWAKEN_DEFAULT_SLIPPAGE=0.005
